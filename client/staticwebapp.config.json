{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/swarmspace/*", "/tasks/*", "/quicktrip/*", "*.{css,js,json,png,jpg,jpeg,gif,ico,svg}"]
  },
  "routes": [
    {
      "route": "/tasks",
      "redirect": "/tasks/"
    },
    {
      "route": "/swarmspace",
      "redirect": "/swarmspace/"
    },
    {
      "route": "/quicktrip",
      "redirect": "/quicktrip/"
    },
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"]
    }
  ],
  "platform": {
    "apiRuntime": "node:20"
  }
}
