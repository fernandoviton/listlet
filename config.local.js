// Local testing configuration - uses mock data instead of API
const CONFIG = {
    API_BASE: 'mock', // Special value to trigger mock mode
    DEFAULT_LIST_NAME: 'tasks'  // Used when no ?list= param in URL
};
