{
  "name": "tasks-api",
  "version": "1.0.0",
  "description": "Azure Function for task list API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.17.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
